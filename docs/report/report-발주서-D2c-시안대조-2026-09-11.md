# 결과보고서 — 발주서 D2c 시안 대조 · 계획 확정 · C0-b (2026-09-11)

- 계획서: `docs/plan/plan-발주서판매처리-D2c.md`
- 상태: **C0-b 완료(브라우저 확인 미완)** · C0-a·c·d 미착수 · 17:50 퇴근 마감으로 중단

---

## 1. 오늘 한 일

### ① 시안 대조 2회 — 낡은 것이 아니라 「근거 소스가 달랐다」

| 회차 | 들어온 것 | 결과 |
|---|---|---|
| 1차 | 매트릭스 6종 갱신(소계 위계 + 택배 발주처 색상) | CSS는 유효, **가용 재고 값 계산이 C0-a 이전 스냅샷** |
| 2차 | `채널별-발주처-수령인-표기규칙.html` + `표기규칙-핸드오프.md` | 지시서는 **엑셀 원본**, 나는 **DB** 실측 → 파서가 버린 행에서 어긋남 |

1차에서 실측 근거로 지적한 셋(택배 95.5%가 발주처≠수령인 · hue 색상 · 「발주 구청」 라벨)이
**2차에 전부 반영되어 돌아왔다.**

### ② 실측 (DB 묶음 #15~#19 + `docs/resources/발주서-통일양식-템플릿.xlsx`)

| 채널 | 발주처 : 수령인 | 지시서 선언 |
|---|---|---|
| 이마트 | 1 : 3 | ✅ |
| 해남급식 | 1 : 2 (시트는 4행 — 0주문 2행은 파서가 버림) | ✅ |
| 서울급식 | 3 : 1 (`여유` 포함) | ✅ |
| 기업별 | 1 : 1 | ✅ |
| 택배 | **9 : 65** · 동일명 3건 | 지시서 「10곳/66행」은 **오독** — 엑셀도 9곳 67행 |

### ③ 사용자 결정

| 결정 | 내용 |
|---|---|
| 시안 반영 순서 | C0에 묶어서 한 번에 |
| 택배 표기 | **전 행 2단이 기본**, 동일명 3건만 한 줄 (1차 시안의 「예외적 2단」 전제 폐기) |
| 발주 0인 행 | **계속 버린다** (지시서 §8 「해남급식 4수령인」 기각 — 파서·스키마 범위) |
| 정렬 | **4종 전환 + `label` 정리** (「가나다」가 실은 발주처 정렬이던 결함 동반 해소) |
| 검증 게이트(§6) | 백로그 |
| 이마트 하단표 | **박스 적재 계산표** → 백로그 §41 (결정 #35와 연결, `unitsPerBox`로 검산 완료) |

### ④ 구현 — C0-b 소계 행 위계

`matrix-client.tsx`: `H_SUM = 26` → **`H_SUM_MAIN = 40` / `H_SUM_SUB = 28`**.
소계 줄 `15px font-extrabold text-foreground`, 가용 줄 `11.5px`. 4행 `top`은 `H_GROUP + H_SPEC + H_SUM_MAIN`.
🔴 **`short`(재고부족 앰버) 판정을 `strong`보다 먼저 유지** — 순서를 바꾸면 강조가 경고를 덮는다(주석으로 박음).

검증: `npx tsc --noEmit` ✅ · `npx eslint` ✅ · **브라우저 미확인**(퇴근 마감).

---

## 2. 주요 발견

1. **`MatrixRow.label`은 죽은 표시값** — 화면은 `vendor`/`recipient`를 직접 조합하고 `label`은 정렬 키로만
   남아 있다. `"발주처 → 수령인"` 문자열이라 「가나다」가 발주처 기준으로 정렬된다. → C0-d에서 제거.
2. **이마트 시트 하단 박스표가 안 들어오는 건 우연** — A열에 값이 있어 `isForeignBlockHeader`를 빠져나가고
   수량이 0이라 `items.length > 0`에서 걸린다. 숫자가 채워지면 주문으로 잘못 적재된다. → 백로그 §41 처방 2.
3. `unitsPerBox`는 스키마·관리화면에만 있고 **환산 계산을 쓰는 코드가 없다.**

---

## 3. 내일 첫 일

1. 🔴 **브라우저에서 C0-b 확인** — `/sales/purchase/15` 소계 줄이 가용 줄보다 확연히 크게 뜨는지,
   **스크롤해도 헤더 4행이 겹치지 않는지**(sticky `top` 누적), 재고부족 칸이 여전히 주황인지
2. C0-a 톤백 열 분리 (`/sales/purchase/19` 시아스가 1,000kg / 200kg 두 열로)
3. C0-c 채널별 2단 표기 + 용어 「수령처」→「수령인」
4. C0-d 정렬 4종 + `rowLabelOf`·`label` 제거

---

## 4. 확인이 필요한 사항

- 이름칸 `104px` 고정폭 — `이마트본사 김보훈`·`울림생협 북가좌점`·`롯데백화점 평촌점`이 잘린다(지시서도 재검토 대상으로 표시)
- 채널 선언(`ChannelDecl`)을 코드 상수로 둘지 관리자 UI로 노출할지
