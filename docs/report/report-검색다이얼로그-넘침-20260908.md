# 결과보고서 — 검색·필터 다이얼로그 가로 넘침 수정

- 날짜: 2026-09-08
- 발단: **모바일 실기기 점검(백로그 §23) 중 사용자 신고** — 제품재고 검색 다이얼로그의
  오른쪽 컬럼(정렬·인증구분·종료일)이 화면 밖으로 잘림
- 범위: 코드 5파일 · 문서 2파일

---

## 1. 증상과 원인

390px에서 다이얼로그 **안쪽 콘텐츠가 11px 넘쳤다.** 페이지 자체는 정상
(`html scrollWidth 390 == clientWidth 390`)이고 **다이얼로그 내부만** 넘쳤다.

```
DIV [dialog-content]     w=358  client=356  scroll=367   ← 11px 넘침
  DIV [grid min-w-0]     w=308  sw=343                   ← 부모 트랙은 308로 정상
    DIV [grid-cols-2]    w=343                           ← 자식이 343으로 부풂
      DIV [space-y-2]    w=163                           ← 146이어야 할 칸이 163
      DIV [space-y-2]    w=163                             (163+163+gap16 = 342)
```

**원인**: `grid` 칸(`div.space-y-2`)에 `min-w-0`이 없었다. grid item의 기본
`min-width: auto`는 **자식의 min-content를 최소폭으로 삼는데**, 그 안의 드롭다운이
`whitespace-nowrap`이라 「재고량 많은순」 글자폭 **163px이 그대로 칸의 하한**이 됐다.
컬럼이 `146×2`로 줄지 못하고 `163×2`로 굳으면서 308px 자리에 343px가 들어간 것이다.

## 2. 진단 과정에서 두 번 헛짚었다 — 기록해 둔다

| 시도 | 가설 | 결과 |
|---|---|---|
| 1차 | `SelectTrigger`의 `w-fit`이 최소폭을 강제한다 | **부분적으로만 맞음.** `w-full min-w-0`으로 고쳤으나 증상 그대로 |
| 2차 | 트랙을 `grid-cols-[minmax(0,1fr)_...]`로 못 박으면 된다 | **틀림.** 클래스는 DOM에 붙었지만 **Tailwind가 CSS를 생성하지 않았다** (콤마 포함 임의값). 실제 트랙은 `auto`로 동작 |
| 3차 | grid 칸에 `min-w-0`(기본 유틸리티) | **해결.** 사용자 실기기 확인 완료 |

🔴 **교훈 1 — 레이아웃 문제는 눈대중으로 두 번 헛짚을 동안 실측 한 번이면 끝났다.**
`clientWidth`/`scrollWidth`와 자식 트리의 `w=`를 찍자마자 범인이 한 줄로 나왔다.
스크린샷 픽셀을 재는 건 스케일·캡처 오차 때문에 신뢰할 수 없다.

🔴 **교훈 2 — 콤마가 든 Tailwind 임의값(`grid-cols-[minmax(0,1fr)]`)은 CSS가 안 만들어진다.**
클래스가 DOM에 보인다고 적용된 게 아니다. `getComputedStyle`로 확인해야 한다.
**기본 유틸리티로 풀 수 있으면 임의값을 쓰지 말 것.**

## 3. 변경 내용

**코드 5파일**

| 파일 | 변경 |
|---|---|
| `components/ui/select.tsx` | `SelectTrigger` `w-fit` → **`w-full min-w-0`** |
| `packages/package-search-dialog.tsx` | grid 칸 6곳 `min-w-0` |
| `raw-stocks/stock-filters.tsx` | grid 칸 6곳 `min-w-0` |
| `raw-stocks/misc/misc-stock-filters.tsx` | grid 칸 6곳 `min-w-0` |
| `milling/milling-filters.tsx` | grid 칸 7곳 `min-w-0` |

`SelectTrigger`의 `w-fit`은 shadcn 기본값(툴바용 컴팩트 select 전제)인데,
이 프로젝트는 **전부 `<Label>` 아래 폼 필드**로 쓴다. `w-full`을 명시하지 않은
15곳을 전수 확인해 **일부러 `w-fit`을 쓰는 곳이 없음**을 확인하고 프리미티브를 바꿨다.
이 한 줄로 도정 필터·원물 입고·잡곡 입고·농가 추가 등 15곳이 함께 정렬된다.

**문서 2파일**
- `docs/모바일-실기기-체크리스트.md` — **§⑥ 검색·필터 다이얼로그 신설**(⑥-1~⑥-6).
  최초 작성 때 **빠져 있던 화면**이라 항목으로 박았다. 재발 시 쓸 콘솔 한 줄도 함께.
- `docs/worklog.md`

## 4. 검증

- `tsc --noEmit` 통과 (eslint는 기존 `set-state-in-effect` 경고 1건, 이번 변경과 무관)
- **사용자 실기기(390px) 확인** — 제품재고 벼 탭 검색 정상, `SelectTrigger`가 바뀐
  다른 화면 6곳도 육안 확인 완료

## 5. 남은 것 / 확인이 필요한 사항

- 체크리스트 **⑥-2~⑥-6 미확인** — 잡곡 탭 검색, 원물재고·잡곡 원물재고·도정관리 필터.
  같은 처방을 선제 적용했으나 **실기기 확인은 아직**이다.
- ①~⑤(차감·재포장·이력·포장하기·공통)는 **여전히 미확인**. 이번 건은 §23의 일부일 뿐이다.
- 같은 `grid-cols-2` + `space-y-2` 패턴이 **다른 다이얼로그에도 있을 수 있다.**
  이번엔 「검색·필터」만 훑었다. 넘침 신고가 또 나오면 그때 전수로 넓힌다.

---

## 6. 2차 — 전수로 넓힌 결과 (같은 날 이어서)

1차는 「검색·필터」만 훑었다. 사용자 요청으로 **`grid-cols` 쓰는 파일 전수**로 넓혔다.

**동일 결함 1곳 추가 발견** — `admin/farmers/farmer-filters.tsx`
`grid-cols-2` 안 MultiSelect 2개. 1차 grep에서 **`grid-cols` 건수가 1이라 순위에 안 걸렸다.**
작목반·품종 이름이 길어지면 똑같이 터지는 자리다. `min-w-0` 2곳 추가.

🔴 **교훈 3 — 건수로 정렬해 상위만 보면 「한 곳뿐인 파일」이 빠진다.**
결함은 빈도가 아니라 패턴으로 찾아야 한다.

**예방 4곳 선제 적용** (사용자 승인)

| 파일 | 현재 상태 | 왜 넣었나 |
|---|---|---|
| `add-stock-dialog` · `add-misc-stock-dialog` | 안전 (옵션 「유기농/무농약/일반」 3글자) | 옵션이 늘 수 있다 |
| `edit-stock-dialog` · `add-farmer-dialog` | **잠재 위험** (옵션이 품종명·작목반명 **동적**) | 작목반명 8자면 넘친다 |

`min-w-0`은 칸이 부풀 때만 작동해 **현재 화면은 바뀌지 않는다.** 비용·위험 0의 예방이다.
자식 클래스가 파일마다 달라(`space-y-2` / `space-y-1.5` / `grid gap-1.5` / `grid gap-2`)
각각에 맞춰 넣었다.

**함께 나왔으나 결함 아님 — 세로 제한(`max-h`) 없음**

검색·필터 다이얼로그 **12곳 전부 `max-h`가 없다.** 차감·재포장·이력은
`max-h-[92dvh] flex flex-col`이 있는데 이쪽은 빠졌다. 길어지면 위아래가 잘리고
**스크롤도 안 된다**(배경은 잠겨 있다). 입력행 6~7개 = 700px 안팎으로 경계선이었으나
**실기기에서 도정 필터 하단 버튼이 정상으로 보였다 → 현시점 결함 아님.**
구조 변경 비용이 증상보다 커서 손대지 않았다. 입력이 늘거나 가로 화면이면 다시 볼 것.

**실기기 확인 완료**: ⑥-1~⑥-6 전부 · ⑤-2(키보드) · 도정 필터 세로

## 7. 최종 변경 (1차 + 2차)

- **코드 10파일** — `select.tsx` 프리미티브 1 + grid 칸 `min-w-0` 9파일
- **문서 3파일** — 체크리스트 §⑥ 신설·종결, 본 보고서, 작업일지
