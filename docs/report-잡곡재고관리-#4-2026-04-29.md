# 잡곡 재고관리 #4 결과보고서 — `/stocks` → `/raw-stocks` 라우팅 이동

> **작업일**: 2026-04-29
> **단계**: `plan-잡곡재고관리.md` §작업 단계 #4
> **계획서**: [plan-잡곡재고관리-#4.md](plan-잡곡재고관리-#4.md)
> **결과**: 코드 정합 검증 완료(tsc 통과). **브라우저 스모크는 사용자 검수 필요**.

---

## 1. 변경 요약

### 디렉터리 rename (git mv 15 파일)
`app/(dashboard)/stocks/*` → `app/(dashboard)/raw-stocks/*`
- 히스토리 보존을 위해 `git mv` 사용
- `milling-cart-context.tsx`도 raw-stocks 디렉터리에 그대로 유지(결정 #2)

### 코드 수정 (총 14 파일)
| 파일 | 변경 |
| --- | --- |
| `app/(dashboard)/layout.tsx` | `MillingCartProvider` import 경로 (`./stocks/` → `./raw-stocks/`) — **사전조사에서 놓친 1곳, 보강 발견** |
| `components/milling-cart-sheet.tsx` | `useMillingCart` import 경로 |
| `app/(dashboard)/milling/stock-list-dialog.tsx` | import 경로 + `router.push('/raw-stocks')` |
| `components/desktop-sidebar.tsx` | `href` + `isActive` |
| `components/mobile-nav.tsx` | `href` + 라벨 `재고` → `원물` (결정 #1) |
| `components/breadcrumb-display.tsx` | `/stocks` 매핑 제거 (결정 #4) |
| `app/actions/audit.ts` | `pathname.startsWith('/raw-stocks')` (결정 #3 옵션 A) |
| `app/actions/admin.ts` | `revalidatePath('/raw-stocks')` 11곳 |
| `app/actions/milling.ts` | 5곳 |
| `app/actions/release.ts` | 4곳 |
| `app/actions/stock.ts` | 4곳 + L336 주석 갱신 |
| `app/actions/stock-excel.ts` | 1곳 |
| `next.config.ts` | `redirects()` 추가 (308 영구 리다이렉트, `/stocks` + `/stocks/:path*`) |

`revalidatePath` 합계: 25곳.

---

## 2. 사전조사 vs 실제 — 누락분 보강

[research-잡곡재고관리-#4.md](research-잡곡재고관리-#4.md)에서 **놓친 사항 1건** 발견 후 보강:

- [app/(dashboard)/layout.tsx:6](../app/(dashboard)/layout.tsx#L6) `import { MillingCartProvider } from "./stocks/milling-cart-context"`
  - 사전조사 grep 패턴이 `'/stocks'` / `\"/stocks\"` / `@/app/(dashboard)/stocks` 위주여서 **상대경로 `./stocks/...`를 못 잡음**
  - tsc 단계에서 즉시 발견(`Cannot find module './stocks/milling-cart-context'`) → 동일 패턴으로 갱신
  - **교훈**: 다음 라우트 이동 시 grep 패턴에 상대경로(`'\./{prev-route}/'`)도 포함

---

## 3. 검증

### 3.1 정적 검증 (완료)
- `npx tsc --noEmit`: **통과** (.next stale typegen 정리 후 0 에러)
- `revalidatePath('/stocks')` 잔존: 0건 (전부 `/raw-stocks`로 치환)
- 잔존 `/stocks` 매칭(non-docs): `next.config.ts` redirects 정의 2줄(의도) + `public/sw.js`(PWA 자동 생성, 다음 빌드 시 갱신) — **무관**
- 모든 import가 `raw-stocks/`로 정합

### 3.2 브라우저 스모크 (사용자 검수 필요)
다음 9개 시나리오를 사용자 환경에서 확인 부탁:

| # | 시나리오 | 기대 |
| --- | --- | --- |
| 1 | `/stocks` URL 직접 입력 | 308 → `/raw-stocks` |
| 2 | `/stocks?varietyId=1&page=2` | 쿼리 보존하며 `/raw-stocks?varietyId=1&page=2` |
| 3 | `/raw-stocks` 페이지 렌더 | 필터·테이블·등록 다이얼로그·엑셀 import/export 정상 |
| 4 | 사이드바·모바일 네비 active 상태 | "원물재고" 활성, 모바일 라벨 "원물" |
| 5 | 헤더 브레드크럼 | "원물재고 · 벼·잡곡 원물 입고 및 도정 전 재고를 관리합니다." |
| 6 | 도정 카트 (검색 → 담기 → 도정 시작) | 정상 (milling-cart-context 경로 검증) |
| 7 | 입고 등록·수정·삭제 | 후 목록 즉시 갱신 (revalidatePath 검증) |
| 8 | 엑셀 import 후 목록 | 즉시 갱신 |
| 9 | 활동 로그 페이지 | Stock 엔티티 변경 내역 분류 정상 (audit.ts 검증) |

`npm run dev` 후 검수 권장.

---

## 4. 본 범위 외 — 다음 단계로 분리

- 잡곡 탭 / 탭 쉘 / 잡곡 입고 다이얼로그 — **#5**
- 사이드바·모바일 네비 전면 개편(메뉴 구조·아이콘) — **#9**. 본 #4는 라우트 의미 정합만(href 1줄 + 모바일 라벨 1줄)

---

## 5. 다음

- 사용자 브라우저 스모크 9 시나리오 확인
- 통과 후 `docs:` 산출물 + 코드 변경 모두 단일 커밋 (`feat: 잡곡 재고관리 #4 — /stocks → /raw-stocks 라우팅 이동`)
- worklog · 메모리 갱신
- 작업 단계 **#5 잡곡 입고 등록** 사전조사로 이동 — 위탁도정/농가도정 sourceType 토글 다이얼로그 + 잡곡 원물재고 탭
