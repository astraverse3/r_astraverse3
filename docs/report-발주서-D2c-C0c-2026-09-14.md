# 결과보고서 — 발주서 매트릭스 C0-c 채널별 2단 표기 · 톤백 라벨 정리 (2026-09-14)

계획서: `docs/plan/plan-발주서판매처리-D2c.md` §C0-c (2026-09-11 확정)
근거: `docs/handoff/발주서판매처리/표기규칙-핸드오프.md`

## 계기

브라우저 확인(C0-a·b·d)에서 사용자가 둘을 짚었다.

1. 톤백 규격 머리가 `톤백 1,000kg`로 뜬다 — 1행 그룹에 이미 `톤백`이 있으니 `1,000kg`만 있으면 된다.
2. 확정해 둔 표기 규칙(`수령인 ｜ 세로선 ｜ 발주처(연한색)`)이 화면에 없다 — 아직 `←발주처` 옛 표기였다.

C0-c는 계획서에 확정만 되어 있고 구현은 이번이 처음이다.

## 변경 사항

| 파일 | 내용 |
|---|---|
| `lib/purchase-channel.ts` | `ChannelDecl` 타입 · `CHANNEL_DECL` 5채널 선언 · `nameTiersOf()` 순수함수 신설 |
| `lib/purchase-channel.test.ts` | 신규 5건 — 채널별 순서 · 서울급식 반전 · 기업별 한 값 · 동일명 한 줄 · 수령인 공백 대체 |
| `app/(dashboard)/sales/purchase/[uploadId]/matrix-client.tsx` | `NameCell` 컴포넌트(앞 값 104px 고정 + 구분선 + 연한 뒤 값) · 열 머리 `decl.columnLabel` · 톤백 2행 라벨 `kg`만 · 「수령처」→「수령인」 |
| `app/(dashboard)/sales/upload-dialog.tsx` · `upload-table.tsx` | 용어 「수령처」→「수령인」(§7) |
| `lib/purchase-order-matrix.ts` · `.test.ts` · `purchase-order-parser.test.ts` · `purchase-order-allocation.ts` | 주석·테스트명 용어 통일 |

## 주요 결정

- **규칙은 하나, 채널은 순서만** — `nameTiersOf`가 `primary`로 앞뒤를 정하고, 뒤가 없거나 앞과 같으면 한 줄.
  서울급식(`발주처 ｜ 수령인`)은 예외 분기가 아니라 같은 함수의 결과다.
- **앞 값 104px 고정 + `flex-none`**, 뒤 값 `min-w-0 truncate` — 구분선이 모든 행에서 같은 x에 선다.
  `W_NAME 184`는 그대로. 뒤 값에 남는 폭은 약 60px라 긴 발주처는 잘리고 `title`로 전체를 보여준다.
- **구분선 색은 Tailwind 토큰**(`border-slate-300` ≈ `#cbd5e1`, `text-slate-500` ≈ `#64748b`)으로 — 핸드오프의 hex를 인라인으로 박지 않았다.
- **`constantSide`는 선언만** — §6 업로드 검증(백로그)이 읽을 자리. 지금 읽는 곳 없음.
- 톤백 라벨: `packageType` 대신 중량만. 「톤백」은 1행 `packagingName`이 이미 보여준다.

## 검증

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | 0 오류 |
| `npx eslint` (변경 5파일) | 0 |
| `npm test` | 262/262 통과 (신규 5건 포함) |
| 브라우저 | **미확인** — 사용자 확인 필요 |

## 확인 필요 (브라우저)

- `/sales/purchase/19` 시아스: 열 머리 `거래처`, 이름 한 줄, 규격 머리 `1,000kg` / `200kg`
- `/sales/purchase/15` 택배: 열 머리 `수령인`, 대다수 행 `수령인 ｜ 발주처` 2단, 구분선 x 정렬,
  동일명 3건(서대문마을생협·예은농산·해남로컬푸드)만 한 줄
- `/sales/purchase/18` 서울급식: 열 머리 `발주처`, `은평구 ｜ 행복플러스` 순서
- `/sales/purchase/17` 이마트 · `/16` 해남급식: 열 머리 `수령인`, `여주점 ｜ 이마트` 순서
- 104px에서 `이마트본사 김보훈` 등 긴 앞 값이 잘리는 정도가 허용 범위인지(계획서 ⚠️)

## 다음

C1~C4 (셀 클릭 → FIFO 배분 팝오버 → `confirmOrderItem` 연결 → 로컬 재계산).
